on:
  pull_request:
    branches:
      - main
      - master

jobs:
  test:
    name: Comment on lint failure
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v2
        with:
          ref: "refs/pull/${{ github.event.number }}/merge"
      - name: Test
        run: echo $(jq --raw-output .pull_request.number "$GITHUB_EVENT_PATH")
      # - name: Search comments
      #   uses: fvictorio/check-changeset@2768f4cb45ca39271acf17f0ff78a959f31f1df2
      #   id: search_comments
      #   with:
      #     issue-number: 
      #     body-includes: foobar
      # - name: Check changeset
      #   run: echo ${{ steps.search_comments.outputs.comment-id }}
